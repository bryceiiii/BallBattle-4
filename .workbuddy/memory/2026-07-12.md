## 2026-07-12

### FRP 内网穿透集成
- 用户已部署 FRP 服务器：公网 `119.91.223.73:32038` → 本地 `127.0.0.1:3000`
- `lanUseHttp` bool → `lanProtocol` 枚举（`Http`/`Https`/`Auto`），Inspector 中为下拉选择器
- `ConnectToLAN` 参数从 `bool useTls` 改为 `LanProtocol protocol`
- LAN 分支用 switch 表达式选择协议：`Http`→强制http / `Https`→强制https / `Auto`→`IsPrivateIP`自动判断
