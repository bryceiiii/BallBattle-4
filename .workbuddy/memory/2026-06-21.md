# 2026-06-21 工作日志

## CircleController HP条 重构
- 移除 `hpBarPrefab` 字段，`CreateHpBar()` 改为 `transform.Find("HpCanvas")` 查找已有子物体
- Fill 通过 `transform.Find("HpBarFill")` 获取引用，新增 `hpFillRect` 缓存
- **强制设置锚点**：HpCanvas → 居中(0.5,0.5)、HpBarFill → 左对齐(0,0)-(0,1)，确保 `sizeDelta` 语义正确，不受预制体锚点影响
- 去掉 Mask 依赖 — Bg 在上层用透明图层覆盖 Fill
