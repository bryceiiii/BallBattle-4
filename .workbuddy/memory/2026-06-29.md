## 2026-06-29

### Ponytail — 砍掉客户端预测，修复抖动
**根因**：客户端预测+修正回环在任何参数组合下都产生可见抖动。SmoothDamp 从不断变化的预测位置向静止的服务端目标收敛 → 来回拉扯。
**决策**：直接砍掉预测。SmoothDamp(0.05s local / 0.15s remote) 本身已经足够平滑。
- 预测消灭的 ~30ms 延迟相比 QuickTunnel 150-300ms RTT 可忽略
- 预测引入的抖动比它消灭的延迟更破坏体验
**改动**：CircleController FixedUpdate 恢复为原始 SmoothDamp，删除 _predictVelocity/currentMass/START_PLAYER_SPEED_PREDICT 等预测变量。localPosSmoothTime 0.03→0.05s 匹配 25Hz 服务器 tick。
**保留**：服务器 25Hz（带宽收益）、QUIC 协议、自动重连、RTT 测量、NetworkDebugDisplay

### PerformanceMonitor 脚本
- 新增 `PerformanceMonitor.cs`，挂载即用
- 平滑 FPS（环形缓冲区均值）+ 三档颜色，卡顿检测用帧间隔阈值 + 延迟清除
- 全部参数 Inspector 可配，两个 Text 拖拽赋值
- **手机端修复**：文件名 → `PerformanceMonitor.cs`（匹配类名），加 `[Preserve]` 防 IL2CPP 裁剪；删除旧 `PerformanceDetect.cs`

### NetworkMonitor 精简 — 删除导出 + 修复显示
- 删除 `ExportFormat` 枚举、`autoExportInterval`、`exportFormat`、`LateUpdate()` 自动导出、`ExportData/ExportCSV/ExportJSON` 三个方法
- `NetworkMonitorHUD.OnDestroy()` 删除导出调用
- `Samples` 列表上限 300 条，`LagEvents` 上限 50 条，防止手机端 OnGUI 每帧遍历数千条数据卡顿

### NetworkMonitor 致命 Bug 修复 — HTTP HEAD 打 WebSocket 导致 100% 误报丢包
**根因**：`DoSampleAsync` 发 HTTP HEAD 请求到 SpacetimeDB WebSocket 端口 3000，WebSocket 不认识 HTTP → 永远返回非 2xx → 100% 走 timeout 分支 → 丢包率 100%
**修复**：删掉 HTTP HEAD 探测，改用纯 TCP connect 计时。TCP 连接成功 = 可达，连接耗时 = 延迟。
**附带修复**：`TotalSamples` 漏掉在 `StartMonitoring()` 重置。
**删除**：`useHttps` 字段、`GetTestUrl()` 方法、`BypassCertificate` 类、`using UnityEngine.Networking`。

### 联机卡顿 P0+P2 修复

**P0 — 服务端吞噬循环内联表查降为字典 O(1)（Lib.cs）**
- 新增 `entitySnapshot` 字典：`entity_id → (circlePlayerId?, foodType?)`
- 在 playerBalls 遍历中同步填充 circle 快照，追加一次 food 遍历填充
- 原吞噬内循环每 entityB 调 2 次 `Find()`（circle + food 表），现改为 `TryGetValue` 单次字典查询
- 内循环从 ~7740 次查表/tick 降为 ~2580 次字典查

**P2 — 客户端 HP 条远程球降频到每 5 帧（CircleController.cs）**
- 加 `_hpFrameCounter`，本地球每帧更新 HP 条，远程球 `_hpFrameCounter % 5 == 0` 才更新
- 对 4 人各 3 球场景：远程 HP 更新从 33 次/帧 → 7 次/帧
