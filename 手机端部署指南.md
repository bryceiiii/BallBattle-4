# BallBattle-4 手机端部署指南

> 将你的 PC 版球球大作战游戏转化为手机版，支持触屏操作和移动网络联机。

---

## 📱 改造概览

| 模块 | PC版 | 手机版 |
|------|------|--------|
| **移动** | WASD 键盘 | 虚拟摇杆（左下角） |
| **瞄准** | 鼠标光标 | 移动方向 = 瞄准方向 |
| **射击** | 鼠标左键 | 射击按钮（右下角，按住连射） |
| **分裂** | 空格键 | 独立分裂按钮 |
| **切弹** | 数字键 1-2 | 弹药切换按钮 |
| **大厅** | 键鼠输入 | 放大触屏UI |
| **HUD** | 标准尺寸 | 放大×1.5~2.5 |

### 新增脚本文件

```
Assets/Scripts/Mobile/
├── VirtualJoystick.cs          # 虚拟摇杆组件
├── MobileInputController.cs    # 手机端输入总控
├── MobileCanvasSetup.cs        # 自动构建触屏UI（摇杆+按钮)
├── MobileHudAdapter.cs         # HUD 手机端尺寸适配
├── MobileLobbyAdapter.cs       # 大厅UI手机适配
├── PlatformInputManager.cs     # 平台检测+自动切换
├── MobileNetworkOptimizer.cs   # 移动网络带宽优化
└── MobileBuildConfig.cs        # Android/iOS 打包配置
```

---

## 🚀 快速开始（3步完成改造）

### 第一步：场景中添加 PlatformInputManager

1. 在 Unity Hierarchy 中创建空 GameObject，命名为 `PlatformInput`
2. 挂载 `PlatformInputManager.cs` 脚本
3. 将场景中已有的 `PlayerInputController` 拖入 `pcInputController` 字段
4. `forceMobileInEditor` 勾选后可在 Editor 中测试手机模式

### 第二步：HUD 适配

1. 找到场景中已有 `HudController` 所在的 GameObject
2. 在同一 GameObject 上挂载 `MobileHudAdapter.cs`
3. 调整缩放系数（默认值即可用）

### 第三步：大厅适配

1. 找到场景中已有 `LobbyUIController` 所在的 GameObject
2. 在同一 GameObject 上挂载 `MobileLobbyAdapter.cs`

完成！运行游戏即可看到：
- **PC端**：无变化，键鼠正常操作
- **手机端**：自动显示触屏UI（摇杆+按钮）

---

## 🔧 架构说明

### 自动平台切换流程

```
游戏启动
   │
   ├── #if UNITY_ANDROID || UNITY_IOS → 手机模式
   │     ├── 创建 MobileCanvasSetup → 自动生成触屏UI
   │     ├── MobileInputController 激活
   │     ├── PlayerInputController 禁用
   │     ├── MobileHudAdapter.ApplyMobileLayout()
   │     └── MobileLobbyAdapter.ApplyMobileLayout()
   │
   └── 否则 → PC模式
         ├── 不创建触屏UI
         ├── PlayerInputController 正常工作
         └── HUD/大厅保持原样
```

### 输入数据流（手机端）

```
虚拟摇杆拖拽
   │
   ▼
VirtualJoystick.Direction (Vector2 归一化)
   │
   ▼
MobileInputController.SendDirection()
   │  去重检查（与上次相同则不发送）
   ▼
SpacetimeDB Reducers.UpdatePlayerDir()
   │
   ▼
服务端 MoveAllPlayer 移动 → 同步到所有客户端
```

### 射击数据流（手机端）

```
按住射击按钮
   │
   ▼
MobileInputController.OnShootButtonDown()
   │  每 shootRepeatInterval 发射一次
   ▼
ShootInAimDirection()
   │  方向 = CurrentDirection（移动方向）
   ▼
SpacetimeDB Reducers.ShootBullet(aimX, aimY, ammoType)
   │
   ▼
服务端生成子弹 → 碰撞检测 → 伤害 → 同步
```

---

## 📊 网络优化（手机端专属）

手机网络（尤其是4G）相比PC宽带具有更高的延迟抖动和有限的带宽。

### 自动启用的优化

| 优化项 | PC | 手机 | 说明 |
|--------|-----|------|------|
| 移动方向发送频率 | 40Hz (25ms) | 20Hz (50ms) | 减少60%发送量 |
| 发送去重 | 值变化才发 | 值变化才发 | 已内置在 SendDirection |
| 网络抖动处理 | SmoothDamp 0.10s | SmoothDamp 0.10s | 远程球插值已有缓冲 |

### 为什么 SpacetimeDB 架构适合手机？

- **服务端权威**：手机断线重连后状态一致，不丢数据
- **Client-Server 模式**：手机不需要做服务端，省电省流量
- **WebSocket 传输**：比传统 UDP 更利于穿透手机蜂窝网络的 NAT
- **增量同步**：SpacetimeDB 只推送变化的行，不是全量推送

---

## ⚙️ 打包配置

### 一键配置（Unity Editor 菜单）

```
Tools → Mobile Build → 📱 配置 Android 打包参数
Tools → Mobile Build → 📱 配置 iOS 打包参数
Tools → Mobile Build → 🔨 快速构建 Android APK
Tools → Mobile Build → 🧹 清理构建缓存
```

### 手动配置要点

| 设置项 | 推荐值 | 原因 |
|--------|--------|------|
| Canvas Scaler Ref Resolution | 1080×1920 | 竖屏9:16基准 |
| 目标帧率 | 60 FPS | 流畅但不过度耗电 |
| 最低 Android SDK | API 26 (8.0) | 覆盖98%+活跃设备 |
| 图形 API | OpenGL ES 3 / Vulkan | 主流手机GPU支持 |
| 屏幕方向 | 竖屏 Portrait | .io游戏标准 |
| 架构 | ARM64 only | 缩小包体 |

---

## 🧪 Editor 中测试手机模式

1. 选中场景中的 `PlatformInput` GameObject
2. 勾选 Inspector 中的 `Force Mobile In Editor`
3. 点击 Play
4. 即可在 Game 视图中看到触屏UI并用鼠标模拟手指操作

### 快捷切换（右键菜单）
- PlatformInputManager → `切换到手机模式`
- PlatformInputManager → `切换到PC模式`

---

## 🐛 常见问题

| 问题 | 解决 |
|------|------|
| 虚拟摇杆不出现 | 检查 PlatformInputManager 是否正确挂载且 forceMobileInEditor=true |
| HUD太小/太大 | 调整 MobileHudAdapter 上的 hpBarScale 等参数 |
| 按钮无响应 | 确保场景中有 EventSystem |
| Android 打包失败 | 检查 Android SDK/NDK 路径（Edit→Preferences→External Tools） |
| 连不上服务器 | 手机和服务端必须在同一网络，或使用云端模式 |
| 射击方向不准 | 手机端射击方向 = 移动方向，摇杆指向即是射击方向 |

---

## 📋 上线前 Checklist

- [ ] Editor 中手机模式测试（移动+射击+分裂+切弹+死亡）
- [ ] 两台手机局域网联机测试（延迟+同步）
- [ ] 手机4G网络下联机测试（延迟抖动）
- [ ] UI 在不同屏幕比例下显示正常（16:9, 18:9, 19.5:9）
- [ ] Android 真机 APK 安装测试
- [ ] 通知权限/存储权限确认（一般不需要）
- [ ] 发热测试（30分钟游戏不异常发热）
- [ ] 断网重连测试（切后台再回来）
