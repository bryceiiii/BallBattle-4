# Handoff: BallBattle-4 网络优化 & 局域网协议自动判断

## Current state

### 网络优化（6/26 ~ 6/29）
经历了三轮迭代，最终状态：

**保留的优化**：
- 服务器 tick rate: 50Hz → 25Hz（`Lib.cs` SERVER_DELTA 0.040f，MoveAllPlayerTimer 40ms）
- 远程插值: SmoothDamp 0.15s（`CircleController.remotePosSmoothTime`）
- 本地插值: SmoothDamp 0.05s（`CircleController.localPosSmoothTime`）
- QUIC 隧道协议（`start-tunnel.ps1` 中 `--protocol quic`）
- 自动重连 3s/10次（`SpacetimeDBNetworkManager`）
- RTT 测量 + F3 调试显示（`PlayerInputController.LastDirSendTime`, `NetworkDebugDisplay`）

**已删除（引入抖动，ponytail 砍掉）**：
- 客户端 Dead Reckoning 预测
- `CircleController` 中 `_predictVelocity`, `currentMass`, `START_PLAYER_SPEED_PREDICT` 等变量
- `Lerp` 修正逻辑

**结论**：预测消灭的 ~30ms 在 QuickTunnel 150-300ms RTT 下可忽略；预测回环引入的抖动比延迟更破坏体验。

### 局域网协议自动判断（本次）
- `SpacetimeDBNetworkManager.Connect()` LAN 分支：`IsPrivateIP(remoteHost)` 判断后选 `http` 或 `https`
- `IsPrivateIP()` 覆盖 RFC 1918 全部私有网段（10.x, 172.16-31.x, 192.168.x）+ loopback 127.x
- 效果：`192.168.1.100` → `http://`，`xxx.trycloudflare.com` → `https://`

### 已修改文件
| 文件 | 改动 |
|------|------|
| `server-csharp/spacetimedb/Lib.cs` | SERVER_DELTA 0.020→0.040, MoveAllPlayerTimer 20→40ms |
| `Client-unity/Assets/Scripts/CircleController.cs` | 移除预测，回归纯 SmoothDamp；localPosSmoothTime 0.05s |
| `Client-unity/Assets/Scripts/SpacetimeDBNetworkManager.cs` | LAN协议自动判断(IsPrivateIP)；自动重连；Connect()状态重置 |
| `Client-unity/Assets/Scripts/PlayerInputController.cs` | LastDirSendTime（RTT测量） |
| `start-tunnel.ps1` | --protocol quic |
| `Client-unity/Assets/Scripts/NetworkDebugDisplay.cs` | 新建，F3 切换 |

## Next steps

1. **重新发布服务器模块 + 重建客户端**，验证局域网 IP 连接和 Tunnel 连接都正常
2. **实测 QuickTunnel 联机**，关注卡顿是否改善（去掉预测抖动后应该明显好转）
3. 如果 RTT 仍然过高（>300ms 持续），考虑换用 VPS 端口转发替代 QuickTunnel
4. 子弹/射击的延迟补偿（目前完全没有，高延迟下命中感差）

## Relevant artifacts

- `.workbuddy/memory/NETWORK_OPTIMIZATION.md` — 完整优化报告
- `.workbuddy/memory/2026-06-26.md` — 详细变更记录
- `.workbuddy/memory/2026-06-29.md` — ponytail 砍预测记录
- `.workbuddy/memory/MEMORY.md` — 项目长期记忆
- `GDD.md` — 游戏设计文档

## Suggested skills

- `ponytail` — 继续精简优化路径，避免过度设计
