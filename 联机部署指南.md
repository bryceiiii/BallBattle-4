# BallBattle-4 多人联机部署指南

> 教你如何用现有 SpacetimeDB 架构和朋友远程联机

---

## 🏗️ 架构概览

```
┌──────────────────────┐
│  SpacetimeDB 服务器   │  ← 一个人运行 (主机)
│  监听 0.0.0.0:3000   │
└──────┬───────────────┘
       │ HTTP / WebSocket
  ┌────┴────────────────┐
  │                     │
  ▼                     ▼
┌──────┐           ┌──────────┐
│ 主机  │           │ 朋友(客户端)│
│ Unity │           │ Unity    │
└──────┘           └──────────┘
```

**核心概念**:
- **主机** = 运行 SpacetimeDB 服务器的人
- **客户端** = 连接到主机服务器的人
- 所有游戏逻辑在服务器端执行，客户端只负责渲染和发送输入
- 这是**服务端权威**架构，防作弊

---

## 🚀 方案一：局域网联机（最简单，适合同一WiFi/网络）

### 主机操作步骤

#### 1. 启动 SpacetimeDB 服务器
双击运行项目根目录下的：
```
启动服务器.bat
```
这会自动：
- 启动 SpacetimeDB 服务器（监听端口 3000）
- 部署游戏模块 `ballbattle4`
- 显示你的局域网 IP 地址

#### 2. 记下你的 IP 地址
脚本会显示类似这样的信息：
```
你的局域网IP可能是:
    192.168.1.105
```
**把这个 IP 发给你的朋友！**

#### 3. 打开 Unity 游戏
在 Unity Editor 中运行游戏（或打包后的exe）：
- 连接模式选择「**本机模式**」
- 点击「启动本机服务器」
- 等待显示「✅ 已连接到服务器」
- 输入你的玩家名，点击「进入游戏」

### 朋友（客户端）操作步骤

#### 1. 打开 Unity 游戏
不需要启动任何服务器！

#### 2. 连接设置
- 连接模式选择「**局域网模式**」
- IP地址栏输入**主机给你的IP**（如 `192.168.1.105`）
- 端口保持 `3000`
- 点击「连接到主机」
- 等待显示「✅ 已连接到服务器」

#### 3. 进入游戏
- 输入玩家名
- 点击「进入游戏」

### 🧯 局域网常见问题

| 问题 | 解决 |
|------|------|
| 连接超时 | 检查双方是否在同一WiFi/局域网 |
| 防火墙阻止 | Windows防火墙 → 允许 spacetimedb-cli.exe 通过 |
| IP不对 | 主机运行 `ipconfig` 确认IPv4地址 |
| 端口被占用 | 修改启动脚本中的 `3000` 为其他端口（如 `3001`） |

---

## ☁️ 方案二：SpacetimeDB 云端联机（适合异地朋友）

### 优点
- 不需要同一网络
- 不需要端口转发
- 24/7 可玩（如果服务器保持在线）

### 主机操作步骤

#### 1. 注册 SpacetimeDB 账号
访问 https://spacetimedb.com 注册账号

#### 2. 登录 CLI
```bash
spacetime login
```

#### 3. 部署模块到云端
```bash
cd server-csharp/spacetimedb
spacetime publish ballbattle4v2
```

#### 4. 修改客户端设置
在 Unity 中：
- 连接模式选择「**云端模式**」
- 模块名确认为 `ballbattle4v2`
- 点击「连接到云端」
- 进入游戏

### 朋友操作步骤
同样的步骤——选择「云端模式」→ 连接 → 输入名字 → 进入游戏

### 云端注意事项
- 云端免费额度有限（查看 SpacetimeDB 定价）
- 模块名 `ballbattle4v2` 要和部署时一致
- 所有连接到同一模块名的玩家在同一个游戏世界

---

## 🌐 方案三：公网联机（端口转发/Ngrok）

适合：主机有公网IP或想用内网穿透工具

### 方式A：端口转发
1. 登录路由器管理页（一般是 `192.168.1.1`）
2. 找到「端口转发/虚拟服务器」
3. 添加规则：内部端口 `3000`，外部端口 `3000`，指向你电脑的局域网IP
4. 查你的公网IP：百度搜索「我的IP」
5. 朋友在游戏里填入**你的公网IP**（不是局域网IP！）

### 方式B：Ngrok（推荐，无需路由器权限）
1. 下载 ngrok：https://ngrok.com
2. 注册并获取 authtoken
3. 启动隧道：
```bash
ngrok http 3000
```
4. ngrok 会给你一个公网地址（如 `https://abc123.ngrok.io`）
5. 先运行启动服务器.bat
6. 朋友连接时：选择「局域网模式」，IP填入 ngrok 给你的域名（去掉 `https://`），端口填 `443`

> ⚠️ ngrok 免费版有带宽限制，2-4人游戏够用

---

## 📋 Unity 场景配置

如果要在 Unity Editor 中使用新的联机大厅UI，需要手动在场景中添加：

### 1. 创建 LobbyCanvas
```
Hierarchy 右键 → UI → Canvas
命名: LobbyCanvas
Render Mode: Screen Space - Overlay
```

### 2. 添加 UI 元素到 LobbyCanvas

创建一个 **Panel**（命名: LobbyPanel），里面包含：

| 元素 | 类型 | 用途 |
|------|------|------|
| Title | Text | 显示 "BallBattle-4 联机大厅" |
| PlayerNameInput | InputField | 玩家名输入 |
| ModeDropdown | Dropdown | 选项: 本机/局域网/云端 |
| LANSettingsGroup | Panel(空) | 包含IPInput和PortInput |
| IPInput | InputField | IP地址输入 (默认 192.168.1.100) |
| PortInput | InputField | 端口输入 (默认 3000) |
| StatusText | Text | 显示连接状态 |
| ConnectButton | Button | 连接按钮 |
| ConnectButtonLabel | Text(子物体) | 按钮文字 |
| EnterGameButton | Button | 进入游戏按钮 |
| HostAddressHint | Text | 显示本机IP提示 |

### 3. 挂载 LobbyUIController 脚本
将 `LobbyUIController.cs` 挂到 LobbyCanvas 上，拖拽所有对应的UI引用。

### 4. SpacetimeDBNetworkManager 设置
场景中已有的 `SpacetimeDBNetworkManager` 的 `Start()` 不再自动连接。
连接由 `LobbyUIController` 中的「连接按钮」触发。

---

## 🔧 技术细节

### SpacetimeDBNetworkManager 连接模式

| 模式 | URI 格式 | 适用场景 |
|------|---------|---------|
| Local | `http://127.0.0.1:3000` | 本机开发/本机运行服务器 |
| LAN | `http://{IP}:{PORT}` | 局域网或公网IP |
| Cloud | `wss://maincloud.spacetimedb.com` | SpacetimeDB 云端 |

### 代码调用方式（编程接口）
```csharp
var net = SpacetimeDBNetworkManager.Instance;

// 本机
net.ConnectLocal("ballbattle4");

// 局域网（指定IP和端口）
net.ConnectToLAN("192.168.1.105", 3000, "ballbattle4");

// 云端
net.ConnectCloud("ballbattle4v2");
```

### 服务器端模块部署
```bash
# 本地部署（服务器已启动）
cd server-csharp/spacetimedb
spacetime publish -s http://127.0.0.1:3000 ballbattle4

# 云端部署
spacetime publish ballbattle4v2
```

---

## 🎮 快速开始 Checklist

- [ ] 主机双击 `启动服务器.bat`
- [ ] 主机记下显示的局域网IP
- [ ] 主机在Unity中选择「本机模式」，连接并进入游戏
- [ ] 朋友在Unity中选择「局域网模式」，填入主机IP
- [ ] 朋友连接并进入游戏
- [ ] 双方看到彼此的角色 → 开始对战！🎉

---

有问题？检查 SpacetimeDB CLI 版本：
```bash
spacetime version
# 应显示 v2.2.0 或更高
```

如果 CLI 未安装：
```powershell
# PowerShell 中运行
iwr https://spacetimedb.com/install -useb | iex
```
